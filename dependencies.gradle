
ext {

    // sdk and tools
    compileSdkVersion = 28

    libVersions = [
            support                     : '1.0.2',
            junit                       : '4.12',
            espresso                    : '3.1.0',
    ]

    libDependencies = [

            // internal libs
            markhor                     : "com.github.shopgun:markhor:2.0.2",
            materialcolorcreator        : project(':MaterialColorCreator'),

            // support libs
            appcompat                   : "androidx.appcompat:appcompat:$libVersions.support",

            // tests
            espresso                    : "androidx.test.espresso:espresso-core:$libVersions.espresso",
            junit                       : "junit:junit:$libVersions.junit",
    ]

}
